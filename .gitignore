# Build outputs (regenerated by the scripts/*.sh scripts — do not commit)
output/
bosh-stemcell-*.tgz
sample-app-release-*.tgz

# Prerequisite ARM64 binaries, fetched/compiled by scripts/build-binaries.sh
# (formerly committed to the repo; now built on demand into this dir)
bin/

# Locally compiled sample-app binary (rebuild with ./build-sample-app.sh)
sample-release/src/sample-app/app

# Go build cache / test artifacts
*.test

# Internal working notes (not for publication)
OSPO-REQUEST.md

# Threat model is maintained on the security review ticket, not in the repo
THREAT_MODEL.md

# Internal code-review reports (not for publication)
semantic-review/

# macOS
.DS_Store
._*
